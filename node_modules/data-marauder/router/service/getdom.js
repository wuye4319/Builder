const cheerio = require('cheerio')

module.exports = class {
  dom(html) {
    if (html) {
      const $ = cheerio.load(html)
      let temphtml = $('.article-content').html()
      let title = $('.article-title').text()
      console.log(temphtml)
    } else {
      console.log('page html is empty!')
    }
  }
}
